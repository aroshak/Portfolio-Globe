# Routes

| URL | Entry | Layout |
|---|---|---|
| `/` | `src/App.tsx` | Single-page application shell |

Vite serves one React SPA. No routing library is currently installed. Supporting pages requested in the redesign will require either a lightweight pathname router or React Router.
